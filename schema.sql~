create table if not exists `person` (
	`id` integer primary key autoincrement,
	`icon` text,
	`name` text,
	`town` text,
	`village` text,
	`home` text, 
	`relation_with_host` text,
	`health_status` text,
	`culture_status` text,
	`tel_number` text,
	`political_status` text,
	`poor_property` text,
	`poor_reason` text,
	`remark` text,
	`labor_export` text,
	`house_reform` text
);

create table if not exists `person` (`id` integer primary key autoincrement, `name` text, `icon` text, `town` text, `village` text, `home` text, `relation_with_host` text, `health_status` text, `culture_status` text, `tel_number` text, `political_status` text, `poor_property` text, `poor_reason` text, `remark` text, `labor_export` text, `house_reform` text);


create table if not exists `village` (
	`id` integer primary key autoincrement,
	`name` text,
	`description` text,
	`team` text,
	`remark` text
);

create table if not exists `village` (`id` integer primary key autoincrement, `name` text, `description` text, `team` text, `remark` text);

insert into `village` (`name`, `description`, `team`, `remark`) values ('新立屯村', '', '', ''), ('双榆树村', '', '', ''), ('一间楼村', '', '', ''), ('贾家屯村', '', '', ''), ('腰营子村', '', '', ''), ('大石桥村', '', '', ''), ('莲花山村', '', '', ''), ('公立亨村', '', '', ''), ('东大门村', '', '', ''), ('冷家店村', '', '', '');

create table if not exists `plan` (
	`id` integer primary key autoincrement,
	`village` text,
	`name` text,
	`content` text,
	`loan` text,
	`self_raised` text,
	`other_fund` text,
	`labor_export` text,
	`house_reform` text,
	`other_plan` text,
	`work_income` text,
	`minimal_assurance_income` text,
	`school_subsidize_income` text,
	`charitable_income` text,
	`business_income` text,
	`compensate_income` text,
	`land_transaction_income` text,
	`family_planning_income` text,
	`pension_income` text,
	`money_income` text,
	`other_income` text,
	`husiness_outcome` text,
	`education_outcome` text
	`medical_outcome` text,
	`marry_outcome` text,
	`other_outcome` text,
	`year` text,
	`remark` text
);

create table if not exists `plan` (	`id` integer primary key autoincrement, `village` text, `name` text, `content` text, `loan` text, `self_raised` text, `other_fund` text, `labor_export` text, `house_reform` text, `other_plan` text, `work_income` text, `minimal_assurance_income` text, `school_subsidize_income` text, `charitable_income` text, `business_income` text, `compensate_income` text, `land_transaction_income` text, `family_planning_income` text, `pension_income` text, `money_income` text, `other_income` text, `husiness_outcome` text, `education_outcome` text `medical_outcome` text,  marry_outcome` text, `other_outcome` text, `year` text, `remark` text);










